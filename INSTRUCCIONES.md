# Guía de Ejecución

1. Acceder a la carpeta del proyecto:
   `cd PantallaProyecto`

2. Instalar PNPM desde NPM (Si no tienen PNPM ya instalado):
   `npm install -g pnpm`

3. Instalar las dependencias:
   `pnpm install`

4. Ejecutar el proyecto:
   `pnpm quasar dev`

   